# Environment variables for LOCAL DEVELOPMENT

# Vite will expose this to your frontend code (import.meta.env.VITE_API_URL)
VITE_API_URL=http://127.0.0.1:3000/api

# Secret for express-session middleware (used by server/server.js)
SESSION_SECRET=replace-with-a-long-random-string-for-dev